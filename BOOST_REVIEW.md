# Обзор: буст morin-filter → Neural Trust (−33% PPL)

**Кому:** коллега (ревью) | **От:** Гера / Hermes | **Дата:** 2026-08-30

---

## Суть за 30 секунд

Morin-filter = корпусный n-граммный prior, смешивается с моделью:
`P = (1−β)·P_model + β·P_corpus` (Jelinek-Mercer). β фиксированный (0.3).

**Мы сделали β обучаемым** — маленький логистический head (6 весов) решает для
каждого контекста, СКОЛЬКО доверять корпусу:

```
β(ctx) = σ(w·[длина_совпадения, log(частота), пик_prior, число_кандидатов, длина_контекста])
```

Результат (код, BPE-512, сглаженная trigram как модель, 5M токенов):

| Конфиг | PPL |
|---|---|
| Модель одна | 28.18 |
| Morin (β=0.3) | 9.65 |
| **Neural Trust** | **6.49 (−33%)** |
| Oracle (лучший β) | 4.45 |

## Почему это работает (механизм)

Диагностика показала: когда prior **находит** совпадение, он почти всегда острый
(пик 0.69) и прав в 61% случаев. НО фиксированный β платит штраф `−log(1−β)` за
каждый случай, где target НЕ в prior (≈39%). При β=0.3 штраф мал (0.36 nats),
при β=0.9 — катастрофа (2.3 nats). Поэтому «всегда доверять» не работает.

**Trust-голова учится:** доверять (β≈0.7-0.9) только длинным/острым/частым
совпадениям, не доверять (β≈0.05) плоским и коротким. Веса после обучения:
`[match_len=1.3, log_tot=1.3, p_top=1.3, n_cand=0.5, ctx_len=−0.1]` — ровно
«доверяй длине, частоте и остроте».

## Честные оговорки

1. **Пока на стенде**, не на настоящей LLM: «моделью» служила сглаженная trigram
   (валидный нижний предел — с сильной моделью выигрыш не меньше, см. ниже).
2. Разрыв до oracle (6.49 → 4.45, ×1.46) — это предел знания «в каком контексте
   target реально в prior». Частично закрывается большим корпусом.
3. Механизм по-прежнему Jelinek-Mercer; новизна не в формуле, а в **обучаемом
   селекторе доверия** поверх неё — prior перестаёт быть статической таблицей
   и начинает масштабироваться с данными.

## Почему с сильной моделью будет не хуже

Morin-смесь помогает тем сильнее, чем слабее модель на локальных паттернах
(код: «что было 3 токена назад» — слепая зона даже для сильных LLM). Оригинал
morin: DeepSeek-Coder 16B, PPL 14.7→9.0 (JS), 7.9→4.0 (Python). Наш стенд
(триграмма 28.2→6.49, 4.3×) — тот же эффект, просто слабее стартовая точка.

## Что нужно от ревью

1. Корректна ли метрика (PPL на честном 4:1 сплите, 10K сэмплов)?
2. Достаточно ли признаков (5) или нужны ещё (энтропия, позиция)?
3. Порядок величины (33%) правдоподобен или пахнет утечкой?
4. Стоит ли портировать на настоящую LLM (llama.cpp пример) первым шагом?

## Ссылки

- Код: `morin-filter/morin_filter.py` → `TrustScorer`
- Полный отчёт: `chaotic-llm/phase01/exp_morin_boost/REPORT.md`
- Валидация: `morin-filter/validate_trust.py` (+ JSON результатов)
